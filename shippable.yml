language: none

build:
  pre_ci:
    - echo "true4"

  ci:
    - ls -al
    - jfrog rt u package.json test-repository-local
 
  pre_ci_boot:
    image_name: drydock/u16pytall
    image_tag: v5.8.2
    pull: true 

  post_ci:
    - echo "true"
    

deploy:
  pre_build:
    - ls

integrations:
  hub:
    - integrationName: jfrognew
      type: artifactory
