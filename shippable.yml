language: node_js
node_js:
  - 0.12
  
build:
  ci:
    - echo "CI section"
    - printenv
    - echo "here"
    - docker run
    - sleep 10000

integrations:
  deploy:
    - integrationName: amazon-keys
      type: amazonKeys
