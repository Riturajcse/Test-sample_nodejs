language: none

build:
  pre_ci:
    - echo "true4"
    - jfrog rt u shippable.yml test-repository-local

  ci:
    - ls -al
    - jfrog rt u package.json test-repository-local
 
  pre_ci_boot:
    image_name: drydock/u16pytall
    image_tag: v5.8.2
    pull: true 

  post_ci:
    - echo "true"
    - jfrog rt u index.js test-repository-local
    

deploy:
  pre_build:
    - ls

integrations:
  hub:
    - integrationName: jfrognew
      type: artifactoryKey
