language: node_js

node_js:
  - 4.3.1
  #- 5.0.0


build:
  #cache: true
  pre_ci:
    - echo "this is pr-ci1"
    - docker pull library/redis
  ci:
    - shipctl bump_version v1.1.1 major
    - shipctl bump_version v1.1.1 minor
    - shipctl bump_version v1.1.1 patch
    - shipctl bump_version v1.1.1-rc.2.beta.2 beta
    - shipctl bump_version v1.1.1-beta.2.rc.2 beta
    - shipctl bump_version 1.1.1-beta.2.rc.2.alpha beta
    - shipctl bump_version v1.1.1 rc 
    - shipctl bump_version 1.1.1-beta.2.rc.2.alpha final
    
#integrations:
  #hub:
    #- integrationName: dockerhub
      #type: generic
      
