language: node_js

build:
  ci:
    - echo "hello"
   
      
integrations:
  notifications:
    - integrationName: slack
      type: slackKey
      recipients:
        - "#mybuilds" 
      on_success: always
      #on_failure: always | change | never
      #on_cancel: always
      #on_start: always
