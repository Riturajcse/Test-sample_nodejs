language: node_js

node_js:
  - 4.3.1
  #- 5.0.0


build:
  #cache: true
  pre_ci:
    - echo "this is pr-ci1"
    - docker pull library/redis
  ci:
    - echo "printing env 12345478798"
    - printenv
    - echo "build-number $BUILD_NUMBER"
    - echo "job-number $JOB_NUMBER"
    - shipctl bump_version v1.1.1 rc 
    
#integrations:
  #hub:
    #- integrationName: dockerhub
      #type: generic
      
