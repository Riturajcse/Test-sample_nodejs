language: node_js
node_js:
  - 0.12
  
build:
  ci:
    - echo "CI section"
    - printenv
    - echo "here"
    - echo $my-integration_INTEGRATION_ACCESSKEY
    - echo my-integration_INTEGRATION_ACCESSKEY
    - echo MY-INTEGRATION_INTEGRATION_ACCESSKEY
    - echo $MY-INTEGRATION_INTEGRATION_ACCESSKEY
    - echo "hh"

resources:
  - name: my-integration
    type: integration
    integration: aws-integration

jobs:
  - name: my-runci-job
    type: runCI
    steps:
      - IN: my-integration
